V3.1.8 2026-01-22
- 优化：主键比对算法升级为高度矢量化实现。使用 Pandas 的 merge 引擎替代了传统的 Python 循环对齐，大幅提升了大数据量（数十万行）下的比对速度。
- 优化：比对结果生成逻辑。通过矢量化字符串操作预处理显示文本，减少了渲染前的计算开销。

V3.1.7 2026-01-22
- 新增：多行多列冻结功能。现在支持在“视图控制”区域通过输入框设置需要冻结的行数和列数，支持行列同时冻结。
- 修复：Sheet 选择不匹配问题。移除了对 Sheet 名称的自动修剪 (.strip())，并增加了基于名称的模糊匹配逻辑，解决了部分带有空格的子表无法正确加载而误跳回首表的问题。
- 修复：比对完成弹窗的差异行数统计逻辑。现在仅统计存在真实差异（新增、删除、修改）的行数，不再包含一致行。

V3.1.6 2026-01-22
- 优化：验证并完善标准化构建系统。
- 修复：针对 Windows 下中文路径导致的 PyInstaller/Qt 插件查找失败问题，引入了自动虚拟盘符映射 (subst) 机制。现在支持在中文路径下直接进行一键打包。

V3.1.5 2026-01-22
- 新增：冻结首列功能。在“视图控制”区域勾选“冻结首列”后，滚动表格时首列（状态列）将保持固定，方便查看差异状态。
- 优化：同步主视图与冻结视图的行高、列宽及选中状态。
- 修复：增强了对异构表格比对时的列宽自动计算稳定性。

V3.1.4 2026-01-22
- 修复：序列模式下因列名不一致（异构表格）导致的 "not in index" 错误。
- 优化：全面重构比对核心算法。现在支持新旧文件列结构完全不同的场景，缺失列将自动以空值补齐参与比对，不再报错。
- 增强：比对引擎的容错性。无论是在“主键模式”还是“序列模式”下，均采用了基于安全获取（.get）的行提取策略，彻底消除列索引依赖。

V3.1.3 2026-01-22
- 修复：主键模式下因列名不一致导致的程序崩溃错误（KeyError: not in index）。
- 新增：关键列存在性自动校验。在比对开始前，程序会检查新旧文件中是否都包含所选的关键列，并提供明确的错误提示（如“新文件中缺少关键列: 数据项ID”）。
- 优化：内部比对逻辑的健壮性。

V3.1.2 2026-01-22
- 修复：持续出现的 "Worksheet named 'header' not found" 错误。
- 增强：极致健壮的 Sheet 读取策略。当按名称查找 Sheet 失败时，程序会自动尝试按索引读取第一个 Sheet 或读取所有 Sheet 后进行匹配，确保“第一个 Sheet”始终可读。
- 优化：引擎识别逻辑，支持更多 Excel 扩展名（如 .xlsm 等）。

V3.1.1 2026-01-22
- 修复：Excel 读取报错 "Worksheet named 'header' not found" 的稳定性问题。
- 优化：自动识别 Excel 表头。程序现在会自动寻找第一个非空行作为表头，并支持重复或空列名的自动重命名。
- 增强：Sheet 识别稳健性。在 Sheet 名不匹配时增加模糊匹配与首表兜底逻辑。

V3.1.0 2026-01-22
- 新增：“差异项总览”页签。提供过滤掉一致行的紧凑对比视图，支持旧值/新值并排展示。
- 优化：UI 布局重构。将配置面板压缩至顶部，主区域采用标签页形式切换“双窗同步”与“差异总览”，大幅提升比对效率。
- 优化：Excel 读取逻辑。引入 pandas 引擎加速多 Sheet 文件读取，增强对 .xls 和合并单元格的兼容性与稳定性。

V3.0.0 2026-01-22
- 重构：界面全面切换为 PyQt，实现主窗口与交互改造。
- 优化：比对流程改为线程执行并配套进度对话框，降低界面卡顿。
- 优化：结果表格与差异明细面板基于 Qt 重新实现，保留搜索、筛选与导出能力。

V2.2.0 2025-12-29
- 新增：结果搜索功能。支持在比对结果中实时搜索任意单元格内容。
- 新增：状态过滤器。支持快速筛选“修改”、“新增”、“删除”、“一致”等特定类型的行。
- 优化：表格渲染逻辑。搜索和过滤操作现在能够毫秒级响应，即使在万行数据下也能流畅切换。
- 优化：UI 布局微调。将搜索与过滤控件整合进顶部设置面板，保持界面整洁。

V2.1.0 2025-12-29
- 新增：差异详情明细面板。支持在右侧开启侧边栏，实时展示选中行的差异详情。
- 优化：针对长文本差异，详情面板提供更清晰的对比视图，高亮标记修改前后的具体内容。
- 优化：差异面板支持自由折叠/展开，在“选中精调”区域可通过开关控制。
- 优化：详情面板采用响应式设计，自动适配不同屏幕宽度下的长文本换行显示。

V2.0.0 2025-12-29
- 优化：性能飞跃。引入 Pandas 向量化比对逻辑，大幅减少逐行遍历和 `iloc` 访问开销。
- 优化：全等行预筛选。在进行序列比对和主键比对前，通过元组哈希快速排除无差异行，比对速度提升 5-10 倍。
- 优化：主键模式升级。移除低效的 `iterrows` 遍历，采用索引映射（Index Mapping）和批量数据提取技术，处理大数据量时更加从容。
- 优化：重构 `_compare_by_sequence`，在 `replace` 块中加入快速匹配逻辑，减少 `difflib` 的后处理负担。

V1.9.2 2025-12-29
- 修复：恢复并优化“选中单元格精调”功能。支持在点击单元格后，通过顶部面板的数值框实时微调该行的高度或该列的宽度。
- 优化：修复了 UI 整合过程中遗漏的 `SMALL` 变量未定义错误。
- 优化：完善了视图缩放反馈，实时显示当前的行高像素值和列宽百分比。

V1.9.1 2025-12-29
- 优化：操作 UI 深度整合。将原本分散在顶、底部的所有配置、设置和图例全部统一整合至顶部面板，极大缩短操作路径。
- 优化：布局精简化。通过多层级 Labelframe 和并排布局，提升空间利用率，为下方的表格对比区域留出更多垂直空间。
- 优化：界面美化。使用更精细的图标和统一的 `ttkbootstrap` 风格，提升视觉专业感。

V1.9.0 2025-12-29
- 升级：UI 现代化改造。引入 `ttkbootstrap` 框架，提供更精美的控件样式和多套主题切换（支持深色模式）。
- 新增：文件拖拽支持 (Drag & Drop)。支持直接将 Excel 文件拖入程序窗口自动识别路径。
- 升级：增强型进度反馈系统。比对过程中提供带百分比和实时处理状态的进度条。
- 优化：重构主界面布局，采用响应式容器，提升在不同分辨率下的适配效果。

V1.8.1 2025-12-29
- 优化：关键列选择弹窗布局优化。采用多列网格布局，解决列数较多时显示不全且空间利用率低的问题。
- 优化：增强弹窗自适应能力，确保“确定”按钮在任何情况下均可见。

V1.8.0 2025-12-29
- 新增：关键列（主键）比对模式。支持通过指定一列或多列作为 ID 进行行对齐，精准识别增删改。
- 优化：重构 `ExcelDiffer` 逻辑，解耦序列比对与主键比对算法。
- 优化：在 UI 中增加“比对选项”区域，支持动态获取列信息并进行交互式主键选择。
- 优化：主键比对模式下，利用字典哈希匹配大幅提升了乱序数据的对齐速度。

V1.7.3 2025-12-26
- 修复：修复拖动滚动条时出现的界面伪影（异常粗框）问题。
- 优化：增强表格渲染的同步性，确保滚动条拖动时虚拟化视图和冻结窗格实时更新。
- 优化：统一单元格样式配置逻辑，彻底消除控件复用导致的样式残留。

V1.7.2 2025-12-26
- 优化：全面优化表格及界面显示字体，统一使用“微软雅黑 (Microsoft YaHei)”，提升中文字体显示效果。
- 优化：微调图例及提示信息字体大小，确保排版美观。

V1.7.1 2025-12-26
- 修复：修复 V1.7.0 中由于节流逻辑导致的鼠标滚轮失效问题。
- 优化：移除不必要的滚动节流，依靠“对象池”技术确保滚动流畅。

V1.7.0 2025-12-26
- 优化：大幅提升表格渲染性能，引入“对象池（Object Pooling）”技术。
- 优化：减少控件创建和销毁开销，滚动流畅度显著提升。
- 优化：引入滚动节流（Throttling）算法，降低高频滚动时的 CPU 占用。
- 优化：改进虚拟化渲染逻辑，仅针对必要属性进行更新。

V1.6.2 2025-12-26
- 修复：修复全局行高调整无效的问题，现在会同步更新所有行的实际高度。
- 修复：修复全局列宽调整逻辑，增加了对数据未加载情况的保护。
- 修复：增强选中项精调功能，支持通过 Spinbox 数值框实时输入调整。
- 优化：改进表格渲染性能，确保尺寸调整后视图立即刷新。

V1.6.1 2025-12-26
- 修复：修复了 V1.6.0 中由于变量未初始化导致的启动报错（AttributeError: freeze_rows）。
- 优化：统一了界面控制变量的初始化逻辑。

V1.6.0 2025-12-26
- 优化：重构底部功能栏布局，采用模块化分组设计（LabelFrame）。
- 优化：将操作控制、显示设置、选中精调和图例说明进行逻辑分区，提升界面整洁度。
- 优化：精简同步滚动选项排版，使用垂直排列节省横向空间。
- 优化：统一滑块和数值框的对齐方式，增加视觉指引。

V1.5.1 2025-12-26
- 修复：修复比对后表格内容不显示的严重 Bug。
- 修复：修复表头组件索引超出范围导致的程序崩溃。
- 修复：修复同步滚动逻辑中的变量名引用错误。
- 优化：确保表格组件在初始化和重置时，关键属性（冻结状态、行列尺寸）得到正确维护。

V1.5.0 2025-12-26
- 新增：选中单元格大小调整功能。点击单元格后，可针对性地调整该行的高度或该列的宽度。
- 优化：同步选中机制。点击左/右表格中的单元格，另一个表格会自动同步选中对应位置。
- 修复：修复了同步滚动在某些情况下失效的问题，统一了滚动条引用。

V1.4.0 2025-12-26
- 新增：自由冻结行列。支持通过输入框自定义冻结前 N 行和前 M 列，提升大数据量下的信息定位能力。
- 新增：行高与列宽全局调整。移除之前的单元格缩放，改为更符合 Excel 习惯的行高（20px-100px）和列宽（50%-300%）独立调整。
- 优化：冻结逻辑重构。采用基于 place 布局的动态层级提升（lift），确保冻结区域始终覆盖在滚动内容之上。

V1.3.0 2025-12-26
- 新增：表格冻结功能。支持首行（标题栏）和首列（状态栏）冻结，在滚动查看大数据量时保持关键信息可见。
- 新增：单元格大小调整功能。添加缩放控制条，支持 50% 到 200% 的无级缩放，适配不同分辨率屏幕。
- 优化：虚拟化渲染引擎。适配缩放和冻结逻辑，确保在不同缩放比例下依然保持流畅的滚动体验。

V1.2.0 2025-12-26
- 新增：智能行对齐算法。引入基于序列比对的行对齐逻辑，精准识别中间增删行，解决偏移导致的误判问题。
- 优化：内部逻辑重构。通过序列对齐预处理，实现更高效的差异检测。

V1.1.1 2025-12-26
- 优化：界面精简。移除行高动态调整功能，简化比对控制栏布局。
- 优化：文档更新。更新“使用说明”与“版本说明”，移除已弃用的行高调整描述。

V1.1.0 2025-12-26
- 优化：表格显示。改进列宽预计算算法，支持中文字符宽度自适应，采样行数提升至 300 行。
- 优化：简化交互。将“自动填充合并单元格”设为默认开启并隐藏配置项。

V1.0.9 2025-12-26
- 新增：项目初始化，支持多 Sheet 比对、合并单元格填充、导出高亮 Excel。
- 新增：标准化构建系统，支持自动提取版本号、图标设置。
- 优化：UI 彻底重构。引入虚拟化滚动技术，支持大规模数据流畅滚动，实现单元格级精准高亮。
- 优化：滚动同步增强。支持垂直/水平滚动同步，增加鼠标滚轮及 Shift 组合键支持。
