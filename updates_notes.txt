V1.6.2 2025-12-26
- 修复：修复全局行高调整无效的问题，现在会同步更新所有行的实际高度。
- 修复：修复全局列宽调整逻辑，增加了对数据未加载情况的保护。
- 修复：增强选中项精调功能，支持通过 Spinbox 数值框实时输入调整。
- 优化：改进表格渲染性能，确保尺寸调整后视图立即刷新。

V1.6.1 2025-12-26
- 修复：修复了 V1.6.0 中由于变量未初始化导致的启动报错（AttributeError: freeze_rows）。
- 优化：统一了界面控制变量的初始化逻辑。

V1.6.0 2025-12-26
- 优化：重构底部功能栏布局，采用模块化分组设计（LabelFrame）。
- 优化：将操作控制、显示设置、选中精调和图例说明进行逻辑分区，提升界面整洁度。
- 优化：精简同步滚动选项排版，使用垂直排列节省横向空间。
- 优化：统一滑块和数值框的对齐方式，增加视觉指引。

V1.5.1 2025-12-26
- 修复：修复比对后表格内容不显示的严重 Bug。
- 修复：修复表头组件索引超出范围导致的程序崩溃。
- 修复：修复同步滚动逻辑中的变量名引用错误。
- 优化：确保表格组件在初始化和重置时，关键属性（冻结状态、行列尺寸）得到正确维护。

V1.5.0 2025-12-26
- 新增：选中单元格大小调整功能。点击单元格后，可针对性地调整该行的高度或该列的宽度。
- 优化：同步选中机制。点击左/右表格中的单元格，另一个表格会自动同步选中对应位置。
- 修复：修复了同步滚动在某些情况下失效的问题，统一了滚动条引用。

V1.4.0 2025-12-26
- 新增：自由冻结行列。支持通过输入框自定义冻结前 N 行和前 M 列，提升大数据量下的信息定位能力。
- 新增：行高与列宽全局调整。移除之前的单元格缩放，改为更符合 Excel 习惯的行高（20px-100px）和列宽（50%-300%）独立调整。
- 优化：冻结逻辑重构。采用基于 place 布局的动态层级提升（lift），确保冻结区域始终覆盖在滚动内容之上。

V1.3.0 2025-12-26
- 新增：表格冻结功能。支持首行（标题栏）和首列（状态栏）冻结，在滚动查看大数据量时保持关键信息可见。
- 新增：单元格大小调整功能。添加缩放控制条，支持 50% 到 200% 的无级缩放，适配不同分辨率屏幕。
- 优化：虚拟化渲染引擎。适配缩放和冻结逻辑，确保在不同缩放比例下依然保持流畅的滚动体验。

V1.2.0 2025-12-26
- 新增：智能行对齐算法。引入基于序列比对的行对齐逻辑，精准识别中间增删行，解决偏移导致的误判问题。
- 优化：内部逻辑重构。通过序列对齐预处理，实现更高效的差异检测。

V1.1.1 2025-12-26
- 优化：界面精简。移除行高动态调整功能，简化比对控制栏布局。
- 优化：文档更新。更新“使用说明”与“版本说明”，移除已弃用的行高调整描述。

V1.1.0 2025-12-26
- 优化：表格显示。改进列宽预计算算法，支持中文字符宽度自适应，采样行数提升至 300 行。
- 优化：简化交互。将“自动填充合并单元格”设为默认开启并隐藏配置项。

V1.0.9 2025-12-26
- 新增：项目初始化，支持多 Sheet 比对、合并单元格填充、导出高亮 Excel。
- 新增：标准化构建系统，支持自动提取版本号、图标设置。
- 优化：UI 彻底重构。引入虚拟化滚动技术，支持大规模数据流畅滚动，实现单元格级精准高亮。
- 优化：滚动同步增强。支持垂直/水平滚动同步，增加鼠标滚轮及 Shift 组合键支持。
